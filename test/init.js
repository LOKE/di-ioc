'use strict';

var expect = require('expect');
var ioc = require('../');

describe('.init()', () => {
  it.skip('should initialise dependencies');
  it.skip('should create a new independent container state each call');
});
